[build-system]
requires = ["setuptools>=82.0.1"]
build-backend = "setuptools.build_meta"

[project]
name = "prediction-agent-arena-client"
version = "0.1.0"
description = "Thin student client for the local prediction-agent-arena API"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["arena_client"]
